# SYLVAN WALLPAPER 付费模块开发要求

## 一、项目背景

当前项目是我的个人壁纸网站：

- 网站名称：SYLVAN WALLPAPER / SYLVAN LAB
- 网站地址：https://www.sylvanwallpaper.cn/
- 网站定位：原创壁纸、同人壁纸、桌搭视觉、外设推荐、视觉定制
- 当前已有内容：
  - 顶部导航栏
  - 首页视觉展示区域
  - 横向壁纸推荐区
  - 热门壁纸分类区
  - 壁纸筛选系统
  - 横屏、竖屏分类
  - 壁纸预览或下载功能
  - 桌搭板块
  - 外设板块
  - 商务合作板块
- 当前整体视觉风格：
  - 深色背景
  - 冷黑、深灰、浅蓝配色
  - 极简科技感
  - 大面积留白
  - 卡片圆角
  - 细描边
  - 低对比度辅助文字
  - 高质感图片展示
  - 轻量动效
  - 桌面端沉浸式视觉布局

现在需要在现有网站基础上增加付费模块。

本次开发最高原则：

> 保留现有网站视觉架构、品牌气质、页面比例、版式逻辑和交互方式，只在现有设计语言中增加付费能力。不得把网站改造成普通商城，不得推翻现有首页重新设计。

---

## 二、Codex 执行身份

你现在是一名具备以下能力的高级全栈开发工程师：

- 前端工程
- 后端工程
- 数据库设计
- 用户认证
- 支付系统
- 数字商品交付
- 网站安全
- 响应式设计
- SEO
- 视觉还原
- 性能优化
- 部署与版本管理

你需要先阅读和分析整个项目，再开始修改。

不得在没有理解现有代码结构、框架和数据来源的情况下直接重写页面。

---

## 三、第一阶段：先分析项目，不要立刻修改

请先完成以下检查：

1. 检查当前项目使用的技术栈。
2. 检查前端框架、路由方式、构建工具和部署方式。
3. 检查壁纸数据目前来自：
   - 本地 JSON
   - 数据库
   - Markdown
   - 静态文件
   - 第三方接口
   - 云存储
4. 检查现有壁纸卡片、筛选器、弹窗和下载逻辑。
5. 检查是否已经存在：
   - 用户系统
   - 数据库
   - API
   - 云存储
   - 管理后台
   - 订单系统
   - 支付代码
6. 检查移动端和平板端适配情况。
7. 检查当前项目环境变量。
8. 检查当前 Git 状态和未提交修改。
9. 检查网站现有视觉组件是否已经组件化。
10. 找出最适合扩展付费功能的位置。

分析完成后，先输出一份简短开发计划，内容包括：

- 当前技术栈
- 当前目录结构
- 需要新增的页面
- 需要新增的数据表
- 需要新增的 API
- 需要修改的已有组件
- 支付接入方案
- 数字商品交付方案
- 预计开发顺序
- 潜在风险

在完成上述分析前，不要删除、重写或大范围移动现有文件。

---

## 四、不可违反的设计约束

### 4.1 必须保留的内容

必须保留现有网站的：

- 顶部导航结构
- 首页整体构图
- 深色视觉风格
- 字体层级
- 页面最大宽度
- 区块间距
- 壁纸卡片比例
- 图片圆角
- 卡片描边
- 横向滚动展示方式
- 分类筛选结构
- 桌面端沉浸式布局
- 现有动效节奏
- 品牌标识
- SYLVAN LAB 相关视觉元素

除非存在明确的功能性错误，否则不要重新设计现有页面。

### 4.2 禁止事项

禁止：

- 将首页改造成传统电商首页
- 使用大面积红色、橙色促销风格
- 增加廉价感倒计时组件
- 增加夸张优惠弹窗
- 使用淘宝式商城布局
- 使用拼多多式价格标签
- 重做现有导航栏
- 大幅改变现有字体
- 大幅改变网站主色
- 随意增加复杂渐变
- 改变现有壁纸卡片宽高比
- 删除现有免费壁纸
- 破坏已有分类筛选
- 将付费信息覆盖在图片主体上
- 将支付密钥写入前端代码
- 在代码中写死真实支付密钥
- 未经确认删除原有数据
- 为实现新功能而直接重构整个项目

### 4.3 视觉延续要求

新增付费模块应继续使用现有设计语言：

- 背景：延续当前深黑、深灰背景
- 主色：延续当前浅蓝、冰蓝强调色
- 边框：低对比度细描边
- 圆角：沿用现有卡片圆角数值
- 阴影：轻量、克制
- 文字：标题高对比，辅助信息低对比
- 按钮：简洁、克制、科技感
- 动效：150ms 至 300ms 的轻量过渡
- 禁止高频闪烁
- 禁止过度动态背景
- 禁止出现与原网站不一致的第三套设计系统

优先复用现有 CSS 变量、Tailwind 配置、主题变量和公共组件。

不得在不同页面重复写大量相同样式。

---

## 五、付费内容体系

网站内容需要分为以下三类。

### 5.1 免费壁纸

免费壁纸继续保留现有下载方式。

需要增加清晰但不突兀的标签：

- 免费
- FREE
- 个人使用

免费壁纸不得因为新增付费功能而被隐藏。

### 5.2 原创付费壁纸

新增“原创套装”或“付费壁纸”类型。

每个付费商品可以包含：

- 商品封面
- 商品名称
- 商品简介
- 售价
- 原价，可选
- 包含壁纸数量
- 支持设备
- 支持分辨率
- 支持比例
- 文件格式
- 文件大小
- 更新日期
- 预览图片
- 是否允许商用
- 使用授权范围
- 购买人数，可选
- 是否推荐
- 是否新品
- 商品状态
- 下载文件地址
- 加密文件地址或私有存储地址

第一阶段建议支持以下价格：

- 单张壁纸：¥3.9 至 ¥9.9
- 小型套装：¥9.9 至 ¥19.9
- 完整套装：¥19.9 至 ¥39.9
- 年度合集：¥59 至 ¥99

价格必须从数据层读取，不要写死在页面组件中。

### 5.3 会员内容

系统数据结构需要预留会员能力，但第一阶段不要求强制上线复杂会员。

预留角色或会员类型：

- 普通用户
- 月度会员
- 年度会员
- 永久会员
- 管理员

会员权益可包括：

- 会员专属壁纸
- 提前下载
- 多比例版本
- 无签名版本
- 高清原图
- 商品折扣
- 定制服务优惠

第一阶段页面中可以暂时显示“会员功能即将开放”，但不能影响普通付费购买流程。

---

## 六、新增页面和入口

### 6.1 顶部导航

在保留现有导航结构的前提下，增加以下入口之一：

推荐命名：

- 原创套装
- 高级壁纸
- 商店
- Premium

优先采用“原创套装”。

新增导航项必须使用现有导航样式，不得单独使用醒目的电商红色。

建议导航结构：

- 壁纸
- 桌搭
- 外设
- 原创套装
- 合作

如果空间不足，可以把用户入口放在右侧。

### 6.2 原创套装列表页

新增页面：

`/premium`

或根据当前项目路由规范命名。

页面结构：

1. 页面标题
2. 简短说明
3. 分类筛选
4. 排序功能
5. 商品卡片网格
6. 分页或懒加载
7. 授权说明
8. 常见问题

筛选项可包括：

- 全部
- 手机壁纸
- 电脑壁纸
- 超宽屏
- 桌搭套装
- 原创角色
- 科幻
- 极简
- 赛博
- 暗黑

排序方式：

- 最新
- 热门
- 价格从低到高
- 价格从高到低

页面视觉应与现有“热门壁纸”区一致。

### 6.3 商品卡片

付费商品卡片需要显示：

- 封面
- 商品名称
- 简短描述
- 价格
- 壁纸数量
- 支持设备
- NEW 或 PREMIUM 标签
- 查看详情按钮

付费标签必须克制。

建议形式：

- 卡片右上角小型浅蓝描边标签
- 图片下方显示价格
- 不要在图片中心覆盖巨大价格
- 不要使用红色促销角标

### 6.4 商品详情页

新增动态详情页面，例如：

`/premium/[slug]`

详情页包含：

1. 商品主预览图
2. 商品名称
3. 商品介绍
4. 售价
5. 包含内容
6. 支持比例
7. 支持设备
8. 分辨率
9. 文件格式
10. 授权说明
11. 购买按钮
12. 登录状态
13. 已购买状态
14. 下载按钮
15. 同系列推荐
16. 常见问题
17. 退款说明
18. 版权说明

已购买用户看到：

- 已购买
- 立即下载
- 查看订单

未购买用户看到：

- 立即购买
- 登录后购买

商品预览图片只能使用经过压缩或带水印的预览图。

不得在前端源码中暴露高清原图真实下载地址。

### 6.5 用户中心

新增用户中心：

`/account`

包含：

- 用户基本信息
- 我的订单
- 已购壁纸
- 下载记录
- 会员状态
- 退出登录

视觉风格继续沿用现有深色卡片设计。

### 6.6 登录页面

新增：

`/login`

第一阶段优先使用：

- 邮箱验证码
- 邮箱魔法链接
- GitHub 登录，可选
- Google 登录，可选

如项目面向中国用户，也需要为后续手机号、微信登录预留扩展接口。

不得自己实现低安全性的明文密码系统。

优先使用成熟认证方案。

### 6.7 购买成功页面

新增：

`/payment/success`

显示：

- 支付成功
- 商品名称
- 订单编号
- 支付金额
- 立即下载
- 返回用户中心

### 6.8 支付取消页面

新增：

`/payment/cancel`

显示：

- 支付未完成
- 返回商品详情
- 重新支付

---

## 七、支付系统设计

### 7.1 支付原则

支付模块必须采用“适配器”设计，不要把整个业务逻辑绑定在单个支付平台上。

建议建立统一接口：

- `createPayment`
- `queryPayment`
- `verifyCallback`
- `refundPayment`
- `closePayment`

后续可替换接入：

- 微信支付
- 支付宝
- Stripe
- 其他数字商品支付服务

### 7.2 当前没有支付密钥时

如果当前项目没有真实支付账户或商户密钥：

1. 不要阻塞整个开发。
2. 先完成完整测试支付流程。
3. 增加 mock payment 模式。
4. 使用环境变量控制：
   - `PAYMENT_PROVIDER=mock`
   - `PAYMENT_PROVIDER=wechat`
   - `PAYMENT_PROVIDER=alipay`
   - `PAYMENT_PROVIDER=stripe`
5. 在 mock 模式下允许模拟：
   - 支付成功
   - 支付失败
   - 支付取消
6. 所有支付逻辑必须支持后续替换真实支付服务。
7. 不要伪造真实支付接入已经完成。

### 7.3 支付安全

必须满足：

- 前端不能决定支付金额
- 后端根据商品 ID 查询真实价格
- 后端生成订单
- 支付回调必须验签
- 支付成功后再发放下载权限
- 不得仅依靠前端跳转判断支付成功
- 订单金额必须校验
- 商品状态必须校验
- 回调必须具备幂等性
- 相同支付通知不得重复发货
- 密钥只能存储在环境变量
- `.env` 不得提交到 Git

---

## 八、订单系统

订单至少需要以下字段：

- `id`
- `orderNumber`
- `userId`
- `productId`
- `productNameSnapshot`
- `productPriceSnapshot`
- `amount`
- `currency`
- `status`
- `paymentProvider`
- `paymentTransactionId`
- `createdAt`
- `paidAt`
- `canceledAt`
- `refundedAt`

订单状态：

- `pending`
- `paid`
- `failed`
- `canceled`
- `refunded`
- `expired`

订单编号需要自动生成，并保证唯一。

商品价格和商品名称需要在下单时保存快照，避免商品后续改价导致历史订单异常。

---

## 九、数字商品交付

### 9.1 下载权限

只有以下用户可以下载付费原文件：

- 已完成支付的购买用户
- 拥有对应会员权限的用户
- 管理员

### 9.2 下载地址

禁止直接把原图或压缩包放在公开静态目录中。

推荐方式：

- 私有对象存储
- 临时签名 URL
- 短期有效下载链接
- 后端鉴权下载接口

下载链接建议：

- 5 至 15 分钟后失效
- 与当前登录用户绑定
- 下载前再次检查订单
- 记录下载日志

### 9.3 下载记录

记录：

- 用户 ID
- 商品 ID
- 订单 ID
- 下载时间
- 下载次数
- IP，可选
- User-Agent，可选

可设置合理下载次数限制，例如：

- 每个订单最多生成 20 次下载链接

不要限制得过于严格，避免用户更换设备后无法正常下载。

---

## 十、数据库设计

根据当前技术栈选择数据库方案。

如果项目当前没有数据库，优先选择适合现有部署环境的方案。

建议至少包含以下数据表。

### 10.1 users

- `id`
- `email`
- `name`
- `avatar`
- `role`
- `membershipType`
- `membershipExpireAt`
- `createdAt`
- `updatedAt`

### 10.2 products

- `id`
- `slug`
- `title`
- `subtitle`
- `description`
- `price`
- `originalPrice`
- `currency`
- `coverImage`
- `previewImages`
- `category`
- `tags`
- `deviceTypes`
- `aspectRatios`
- `resolutions`
- `fileFormat`
- `fileSize`
- `wallpaperCount`
- `licenseType`
- `isPublished`
- `isFeatured`
- `isNew`
- `productType`
- `createdAt`
- `updatedAt`

### 10.3 product_files

- `id`
- `productId`
- `fileName`
- `storageKey`
- `fileSize`
- `fileType`
- `resolution`
- `aspectRatio`
- `deviceType`
- `createdAt`

### 10.4 orders

按照订单系统要求设计。

### 10.5 purchases

- `id`
- `userId`
- `productId`
- `orderId`
- `createdAt`

### 10.6 downloads

按照下载记录要求设计。

### 10.7 memberships

预留会员系统使用。

数据库操作需要使用当前项目适配的 ORM 或数据访问层。

不得在组件中直接拼接数据库 SQL。

---

## 十一、管理功能

第一阶段不要求开发复杂后台，但需要至少提供一种安全的商品管理方式。

优先顺序：

1. 如果项目已有后台，在现有后台增加商品管理。
2. 如果项目使用 CMS，在 CMS 中增加商品模型。
3. 如果项目数据来自 JSON，可先建立结构化商品数据文件。
4. 如果有数据库，可以增加受保护的简易管理页面。

管理功能至少支持：

- 新增商品
- 修改商品
- 上下架
- 修改价格
- 添加预览图
- 上传付费文件
- 设置标签
- 设置推荐状态
- 查看订单
- 查看购买用户
- 查看下载记录

管理入口不得对普通用户开放。

需要进行管理员身份校验。

---

## 十二、现有壁纸系统兼容要求

现有壁纸数据不能被破坏。

建议给现有壁纸增加字段：

- `accessType: free | paid | member`
- `productId`，可选
- `downloadType`
- `licenseType`

免费壁纸：

```json
{
  "accessType": "free"
}
```

付费壁纸：

```json
{
  "accessType": "paid",
  "productId": "对应商品ID"
}
```

会员壁纸：

```json
{
  "accessType": "member"
}
```

如果现有数据结构不适合，应该增加兼容层，而不是直接全部重写。

---

## 十三、版权和授权展示

网站需要增加以下说明页面或弹窗：

- 使用许可
- 版权说明
- 退款说明
- 用户协议
- 隐私政策

付费商品需要明确区分：

- 个人使用授权
- 商业使用授权
- 不允许二次销售
- 不允许重新打包
- 不允许上传至素材平台
- 不允许冒充原创作者
- 不允许用于训练公开模型，可选

对于包含第三方影视、动漫或游戏角色的同人壁纸：

- 默认保留为免费展示或个人使用内容
- 不自动加入付费商品
- 不提供商业授权
- 付费区优先使用拥有完整权利的原创内容

不要擅自将现有全部同人壁纸设置为付费。

---

## 十四、SEO 要求

新增页面必须支持：

- 独立 title
- 独立 description
- Open Graph 信息
- 分享封面
- Canonical URL
- sitemap
- robots
- 商品结构化数据
- 面包屑
- 图片 alt
- 可索引的商品详情页面

如果当前壁纸内容完全依赖客户端 JavaScript 加载，需要检查搜索引擎是否能够读取。

如当前框架支持，应使用：

- SSR
- SSG
- 预渲染
- 动态元数据

不要为了 SEO 重写整个项目，只解决当前无法抓取的关键问题。

---

## 十五、数据统计

增加以下埋点能力：

- 商品曝光
- 商品点击
- 查看详情
- 点击购买
- 创建订单
- 支付成功
- 支付失败
- 下载成功
- 外设链接点击
- 定制服务咨询

优先复用项目已有统计工具。

如果没有统计工具，建立轻量事件封装，不要把统计代码散落在所有组件中。

---

## 十六、响应式要求

必须检查：

- 1920px 桌面端
- 1440px 桌面端
- 1366px 桌面端
- 平板端
- 430px 手机端
- 390px 手机端
- 375px 手机端

要求：

- 桌面端保持当前视觉密度
- 手机端商品卡片不溢出
- 支付按钮可以正常点击
- 弹窗不超出屏幕
- 商品详情图片可以滑动
- 订单信息不横向溢出
- 导航在移动端合理折叠
- 不破坏现有壁纸浏览体验

---

## 十七、性能要求

需要进行：

- 图片懒加载
- 缩略图和原图分离
- 预览图压缩
- 路由级代码拆分
- 避免重复请求
- 商品列表缓存
- 防止首屏加载全部高清壁纸
- 避免在列表中请求私有下载链接
- 优化横向滚动性能

目标：

- 不因为新增付费功能明显降低首页加载速度
- 不让支付 SDK 阻塞首页
- 支付相关代码仅在需要时加载

---

## 十八、错误处理

需要处理以下情况：

- 用户未登录
- 商品不存在
- 商品已下架
- 商品价格变化
- 重复创建订单
- 支付失败
- 支付超时
- 支付回调重复
- 已购买商品再次购买
- 下载链接失效
- 文件不存在
- 数据库连接失败
- 网络中断
- 支付接口暂时不可用

错误提示必须符合当前网站风格。

不要直接在页面中显示原始服务器错误堆栈。

---

## 十九、测试要求

### 19.1 功能测试

- 免费壁纸正常下载
- 付费壁纸不能绕过支付下载
- 未登录不能购买
- 登录后可以创建订单
- mock 支付成功后可以下载
- mock 支付失败后不能下载
- 已购商品显示已购买
- 用户中心显示正确订单
- 下载链接会失效
- 商品下架后无法新购买
- 历史购买用户仍可访问已购内容

### 19.2 安全测试

- 修改前端价格不能改变订单金额
- 修改商品 ID 不能下载未购买内容
- 直接访问文件地址无效
- 普通用户不能访问管理功能
- 回调接口具备幂等性
- 环境变量没有暴露到前端

### 19.3 UI 测试

- 与现有网站视觉一致
- 首页没有明显变形
- 导航没有错位
- 卡片尺寸一致
- 手机端正常
- 平板端正常
- 深色模式下文字清晰

### 19.4 构建测试

执行项目已有的：

- lint
- typecheck
- test
- build

修复由本次修改引入的错误。

不得通过关闭类型检查或删除测试来让构建通过。

---

## 二十、开发顺序

请严格按照以下顺序实施。

### 阶段一：安全准备

1. 检查 Git 状态。
2. 创建新分支：`feature/premium-module`
3. 备份重要配置。
4. 不提交 `.env`。
5. 记录当前可正常运行版本。

### 阶段二：数据结构

1. 增加商品数据模型。
2. 增加订单模型。
3. 增加购买记录。
4. 增加下载记录。
5. 增加用户权限字段。
6. 增加数据库迁移。

### 阶段三：界面

1. 新增“原创套装”导航入口。
2. 新增商品列表页。
3. 新增商品详情页。
4. 新增登录页面。
5. 新增用户中心。
6. 新增订单状态页面。
7. 给现有壁纸卡片增加免费或付费标签。

### 阶段四：业务逻辑

1. 登录鉴权。
2. 创建订单。
3. mock 支付。
4. 支付状态更新。
5. 发放购买权限。
6. 下载鉴权。
7. 下载记录。

### 阶段五：真实支付适配层

1. 建立支付 Provider 接口。
2. 保留 mock 实现。
3. 为微信支付、支付宝或其他支付平台预留实现目录。
4. 添加环境变量示例。
5. 编写支付配置说明。

### 阶段六：测试与优化

1. 功能测试。
2. 响应式测试。
3. 安全检查。
4. 构建检查。
5. 性能检查。
6. SEO 检查。

---

## 二十一、MVP 首发商品

请先创建一个测试用原创付费商品。

商品示例：

- 商品名称：SYLVAN DROP 01
- 英文副标题：REDLINE CYBER
- 商品类型：原创壁纸套装
- 售价：¥19.9
- 原价：¥29.9
- 壁纸数量：12 张
- 支持设备：
  - 手机
  - 电脑
  - 超宽屏
- 支持比例：
  - 9:16
  - 3:4
  - 16:9
  - 21:9
  - 32:9
- 文件格式：
  - JPG
  - PNG
- 授权：
  - 个人使用
  - 禁止二次销售
  - 禁止商业使用
- 状态：测试商品
- 支付模式：mock

如果项目中没有真实商品图片，可以先使用统一占位图片，但必须清楚标记为测试数据。

不要使用现有第三方角色壁纸作为该测试付费商品。

---

## 二十二、环境变量

根据最终技术栈生成 `.env.example`。

建议包含：

```env
DATABASE_URL=
AUTH_SECRET=
APP_URL=
PAYMENT_PROVIDER=mock

WECHAT_PAY_APP_ID=
WECHAT_PAY_MCH_ID=
WECHAT_PAY_API_KEY=
WECHAT_PAY_PRIVATE_KEY=

ALIPAY_APP_ID=
ALIPAY_PRIVATE_KEY=
ALIPAY_PUBLIC_KEY=

STRIPE_SECRET_KEY=
STRIPE_WEBHOOK_SECRET=

STORAGE_PROVIDER=
STORAGE_BUCKET=
STORAGE_REGION=
STORAGE_ACCESS_KEY=
STORAGE_SECRET_KEY=
```

不要在 `.env.example` 中写入真实密钥。

只保留字段名称和说明。

---

## 二十三、代码质量要求

要求：

- 使用 TypeScript，如果当前项目支持
- 不滥用 `any`
- 提取公共类型
- 提取公共组件
- 提取支付服务层
- 提取订单服务层
- 提取下载服务层
- 不在 UI 组件中直接处理复杂数据库逻辑
- 不在组件中直接写支付签名逻辑
- 增加必要注释
- 命名清晰
- 保持原项目代码风格
- 避免无意义的大规模格式化
- 不修改与本任务无关的文件

---

## 二十四、提交要求

请将开发过程拆分为合理提交。

建议提交：

1. `feat: add premium product data model`
2. `feat: add premium product pages`
3. `feat: add authentication and account pages`
4. `feat: add order and mock payment flow`
5. `feat: add protected digital downloads`
6. `feat: add premium module responsive styles`
7. `docs: add payment and deployment guide`

不要把所有修改压缩成一个难以审查的巨大提交。

---

## 二十五、完成后必须输出的报告

完成开发后，输出一份中文报告，必须包含以下内容。

### 已完成功能

列出本次实际完成内容。

### 修改文件

列出新增和修改的主要文件。

### 数据库变化

列出新增数据表、字段和迁移方式。

### 页面地址

列出：

- 原创套装列表页
- 商品详情页
- 登录页
- 用户中心
- 支付成功页
- 支付取消页

### 支付状态

明确说明：

- 当前是 mock 支付还是真实支付
- 真实支付还缺少哪些配置
- 如何切换支付 Provider

### 环境变量

列出需要用户填写的环境变量。

### 测试结果

说明：

- lint 是否通过
- typecheck 是否通过
- build 是否通过
- 功能测试是否通过

### 未完成事项

不得隐瞒未完成内容。

### 下一步操作

给出部署、数据库迁移、绑定支付账户和上传商品文件的具体步骤。

---

## 二十六、出现问题时的处理方式

如果出现以下情况，不要擅自大改：

- 无法识别当前数据库
- 当前项目没有后端
- 当前部署环境不支持服务端 API
- 当前项目无法安全保存支付密钥
- 当前项目是纯静态站
- 现有登录系统无法扩展
- 支付平台需要用户商户资料
- 缺少云存储账户
- 缺少真实商品文件

此时先：

1. 明确说明发现的问题。
2. 给出两至三种兼容方案。
3. 推荐最少改动方案。
4. 继续完成不依赖外部资质的部分。
5. 不要因为缺少支付账户停止所有开发。
6. 使用 mock 支付完成完整业务闭环。

---

## 二十七、最终目标

最终结果应满足：

1. 原网站视觉结构基本不变。
2. 免费壁纸功能继续正常。
3. 网站增加自然、克制的原创付费入口。
4. 用户可以登录。
5. 用户可以查看付费商品。
6. 用户可以创建订单。
7. 用户可以完成测试支付。
8. 支付成功后获得下载权限。
9. 付费原文件无法通过公开地址直接访问。
10. 用户可以在个人中心查看已购内容。
11. 后续能够平滑接入微信支付或支付宝。
12. 新增功能不会让网站变成普通电商商城。
13. 新增页面在桌面端和手机端均可正常使用。
14. 项目可以正常构建和部署。

---

# 附录一：给 Codex 的第一条执行消息

将本文件放在项目根目录后，把下面内容发送给 Codex：

```text
请读取项目根目录中的《付费模块开发要求.md》，并严格按照其中的步骤执行。

本次任务的最高优先级是保留现有网站的视觉架构、页面比例、壁纸浏览体验和品牌风格，不允许推翻首页重新设计。

请先全面检查当前项目的技术栈、目录、数据来源、路由、部署方式、登录系统、数据库和现有下载逻辑。

第一轮只需要完成以下事项：

1. 输出当前项目技术栈。
2. 输出现有目录结构分析。
3. 说明现有壁纸数据来自哪里。
4. 说明付费模块最适合采用什么技术方案。
5. 列出预计新增和修改的文件。
6. 列出数据库和支付系统方案。
7. 指出可能破坏现有网站的风险。
8. 给出分阶段开发计划。

暂时不要修改代码，不要安装依赖，不要创建数据库，不要重构现有页面。等我确认方案后再开始正式开发。
```

---

# 附录二：确认方案后的第二条执行消息

```text
方案确认，可以开始开发。

请创建独立 Git 分支 feature/premium-module，并按照《付费模块开发要求.md》分阶段实施。

先完成 MVP：

1. 保留现有首页和视觉架构。
2. 新增“原创套装”导航入口。
3. 新增付费商品列表页。
4. 新增商品详情页。
5. 新增用户登录。
6. 新增用户中心。
7. 新增订单系统。
8. 新增 mock 支付流程。
9. 新增已购商品下载鉴权。
10. 新增一件 ¥19.9 的测试原创壁纸套装。
11. 保持现有免费壁纸下载功能不变。
12. 不接入真实支付密钥。
13. 不使用现有第三方影视或动漫角色壁纸作为测试付费商品。

每完成一个阶段，都先运行项目并检查页面，再进行下一阶段。不要为了实现功能而重写整个项目。
```

---

# 附录三：执行过程中的补充约束

如果 Codex 在执行过程中准备进行以下操作，必须先停止并说明原因，等待确认：

- 删除现有首页或核心组件
- 更换前端框架
- 更换部署平台
- 删除现有壁纸数据
- 大规模重构路由
- 修改全部全局样式
- 引入新的完整 UI 框架
- 接入真实支付账户
- 修改域名或生产环境配置
- 执行不可逆数据库迁移
- 将原图移动到公开目录
- 将支付密钥写入仓库

默认优先采取最少改动、可回滚、可验证的实现方案。
